name: "Solve simple MPS with HiGHS"
command: "highs model.mps"
assertions:
  - type: exit_code
    expected: 0
  - type: output_contains
    stream: stdout
    pattern: "Model status        : Optimal"
  - type: output_contains
    stream: stdout
    pattern: "Objective value     :  5.0"
